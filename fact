#include <stdio.h>
long int fact1(int);
int main()
{
    long int n;
    scanf("%ld",&n);
    printf("%ld",fact1(n));

    return 0;
}
long int fact1(int a)
{
    if (a >=1)
    return a*fact1(a-1);
    else
    return 1;

}
